DATASET:
  TRAIN: "data/pretrain_data_old.json"
  VAL: 'data/fine_tune/test_data.json'

MODEL:
  NAME: "leftnet"
  PRETRAINED_MODEL_PATH: "saved_models/pretrain/leftnet_z/best.ckpt"
  INIT_FILE: "cif_file/atom_init.json"
  CIF_FOLDER: "cif_file"


SOLVER:
  LR: 0.001
  EPOCHS: 200


LEFTNET:
  ENCODING: 'z'

LOGGING:
  LOG_DIR: "saved_models"
  LOG_DIR_NAME: "pretrain/leftnet_z"
