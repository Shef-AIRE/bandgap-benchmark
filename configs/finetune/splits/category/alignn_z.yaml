DATASET:
  TRAIN: ''
  VAL: ''
  PREDEFINED_SPLIT: true
  SPLIT_GLOB: data/split_by_category/*.train.json
MODEL:
  NAME: alignn
  PRETRAINED_MODEL_PATH: saved_models/pretrain/alignn_z/best.ckpt
  INIT_FILE: cif_file/atom_init.json
  CIF_FOLDER: cif_file
ALIGNN:
  ENCODING: z
SOLVER:
  LR: 0.001
  EPOCHS: 100
  NUM_FOLDS: 10
LOGGING:
  LOG_DIR: saved_models
  LOG_DIR_NAME: split_by_category/alignn_z
