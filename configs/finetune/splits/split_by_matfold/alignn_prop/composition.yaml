DATASET:
  TRAIN: ""
  VAL: ""
  PREDEFINED_SPLIT: True
  SPLIT_GLOB: "data/domain_split/composition_split/*.train.json"

MODEL:
  NAME: "alignn"
  PRETRAINED_MODEL_PATH: "saved_models/pretrain/alignn_prop/best.ckpt"
  INIT_FILE: "cif_file/atom_init.json"
  CIF_FOLDER: "cif_file"

ALIGNN:
  ENCODING: "prop"

SOLVER:
  LR: 0.001
  NUM_FOLDS: 10
  EPOCHS: 50


LOGGING:
  LOG_DIR: "saved_models"
  LOG_DIR_NAME: "split_json/alignn_prop/composition"
