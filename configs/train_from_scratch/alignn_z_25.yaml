DATASET:
  TRAIN: "data/fine_tune/train_data_25.json"
  VAL: "data/fine_tune/test_data.json"

MODEL:
  NAME: "alignn"
  PRETRAINED_MODEL_PATH: ""
  INIT_FILE: "cif_file/atom_init.json"
  CIF_FOLDER: "cif_file"

SOLVER:
  LR: 0.001
  NUM_FOLDS: 10
  EPOCHS: 100

ALIGNN:
  ENCODING: "z"

LOGGING:
  LOG_DIR: "saved_models"
  LOG_DIR_NAME: "train_from_scratch/alignn_z_25"
